[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "npm install -g bitget-hub",
  "npm install -g @bitget-ai/getagent-skill"
]

[start]
cmd = "python main.py & uvicorn dashboard.app:app --host 0.0.0.0 --port $PORT"